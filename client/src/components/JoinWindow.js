import React from 'react';
import logo from '../images/logo.png';

const JoinWindow = () => {
    return (
        <div>
            <img src={logo} />
        </div>
    )
}

export default JoinWindow;